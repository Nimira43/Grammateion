import { Card, CardHeader, CardTitle } from '@/components/ui/card'

export function AllTransactions({
  month,
  year
}: {
  month: number
  year: number
}) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>

        </CardTitle>
      </CardHeader>
    </Card>
  )
}